# REQUIREMENTS

## 1. 프로젝트 목표
- `../jwreview` 프로젝트를 리팩토링하여 구조/가독성/유지보수성을 개선한다.
- 기존 MongoDB Community 설치형(온프레미스/로컬) 의존을 제거하고, 클라우드 MongoDB를 사용하도록 전환한다.
- `npx createLithent`의 SSR 보일러플레이트를 기반으로 기본 구조를 재정비한다.
- UI는 기존 `../jwreview`의 카드 리스트 + 상세뷰 패턴을 유지하되, 더 세련된 디자인으로 개선한다.
- 마크다운 뷰어는 GitHub 스타일을 참고한 읽기 좋은 화면으로 개선하고, 라이트/다크 모드 전환을 지원한다.

## 2. 해결하려는 문제
- `../jwreview`가 기존 설치형 MongoDB 기준으로 동작하고 있어 클라우드 환경 표준과 맞지 않는다.
- 관련 프로젝트(`../memo`)는 이미 클라우드 MongoDB 기준으로 동작하도록 정리되어 있어, 두 프로젝트 간 운영 기준을 맞출 필요가 있다.
- 특정 카테고리 필터(예: `review`)가 코드에 고정되면 운영 중 변경이 어렵다.

## 3. 대상 사용자
- 1차 대상은 개인 1인 운영 사용자(프로젝트 소유자)다.
- PC와 모바일을 모두 자주 사용하며, 모바일에서 리스트/상세 조회 빈도가 높다.

## 4. 핵심 사용 시나리오
- 사용자는 카드 리스트에서 원하는 항목을 빠르게 찾고 상세 마크다운을 읽는다.
- 모바일 환경에서도 리스트 탐색, 상세 읽기, 라이트/다크 모드 전환을 자주 수행한다.
- 운영자는 `.env`의 카테고리 필터 값을 변경해 코드 수정 없이 조회 대상을 바꾼다.

## 5. 기능 요구사항
- `../jwreview`의 DB 연결 대상을 MongoDB Community 설치형에서 클라우드 MongoDB로 변경한다.
- 클라우드 MongoDB 접속에 필요한 설정(URI, 인증, 환경변수, 연결 옵션)을 코드/설정에 반영한다.
- `../memo` 프로젝트의 마이그레이션 완료 상태 및 연결 방식을 레퍼런스로 삼아 `../jwreview`에 적용한다.
- 기존 핵심 기능이 클라우드 DB 전환 이후에도 동일하게 동작해야 한다.
- 초기 프로젝트 세팅은 `npx createLithent`로 생성한 SSR 버전 보일러플레이트를 기준으로 구성한다.
- CSS 프레임워크는 Tailwind CSS를 사용한다.
- 메인 화면은 카드 리스트와 상세뷰(선택 항목 상세 확인) 구조로 구성한다.
- 카드 리스트 항목에는 `title`, `tag`, `date` 정보를 노출한다(기존 `../jwreview` 수준).
- 카드 항목은 카드 전체가 클릭 영역이어야 한다.
- MongoDB 리스트 조회 시 카테고리(태그) 필터를 적용할 수 있어야 한다.
- 기본 필터 값은 `../jwreview`의 기존 기준인 `review`를 참고하되, 실제 값은 `.env`에서 변경 가능해야 한다.
- 카테고리 필터 값 변경 시 코드 수정 없이 환경변수 변경만으로 반영되어야 한다.
- 마크다운 뷰어 페이지에서 라이트 모드/다크 모드 전환 기능을 제공해야 한다.
- 마크다운 본문 스타일은 GitHub 마크다운 스타일을 차용하여 제목/본문/코드블록/리스트/표 가독성이 높아야 한다.
- 마크다운 뷰어는 모바일/태블릿/PC에서 반응형으로 동작해야 한다.
- PC 화면에서는 뷰어 컨텐츠 최대 너비를 제한하여 읽기 좋은 레이아웃을 유지해야 한다.

## 6. 비기능 요구사항
- 성능: DB 전환 후 기존 대비 체감 성능 저하가 없어야 한다.
- 보안: DB 인증정보는 코드 하드코딩 없이 환경변수/보안 설정으로 관리한다.
- 안정성/운영: 재시작/배포 후에도 클라우드 DB 연결이 안정적으로 유지되어야 한다.
- UI/디자인: 기존 `../jwreview` 대비 더 세련되고 일관된 시각 스타일을 제공해야 한다.
- UI 톤: 라이트/다크 모드 모두 차분한 톤의 색상 체계를 사용해야 한다.
- 타이포그래피: 모바일/PC 가독성을 고려한 권장 폰트/크기 체계를 적용해야 한다.
- 운영 편의성: 카테고리 필터 정책을 배포 없이 설정(`.env`)으로 조정할 수 있어야 한다.
- 접근성/가독성: 라이트/다크 모드 모두에서 텍스트 대비와 가독성이 충분해야 한다.
- 반응형 품질: 화면 크기 변화 시 레이아웃 깨짐 없이 일관된 읽기 경험을 제공해야 한다.
- 모바일 사용성: 모바일에서도 리스트 탐색/상세 읽기/테마 전환이 불편 없이 동작해야 한다.

## 7. 범위
- 포함: `../jwreview` 리팩토링, DB 연결 계층/설정 전환, `../memo` 레퍼런스 기반 적용, `createLithent` SSR 보일러플레이트 기반 기본 세팅, Tailwind 기반 UI 재구성(카드 리스트 + 상세뷰).
- 제외: 클라우드 DB로의 원천 데이터 마이그레이션 작업 자체(이미 마이그레이션된 상태를 사용).

## 8. 제약 조건
- 기술/기간/인력/예산: 기존 코드베이스를 유지한 점진적 리팩토링으로 진행하고, `../memo` 구현 패턴을 우선 참고한다.
- 기술 스택: SSR 기반 Lithent 보일러플레이트 + Tailwind CSS를 기본 전제로 한다.

## 9. 완료 기준 (Definition of Done)
- `../jwreview`가 설치형 MongoDB 없이 클라우드 MongoDB만으로 실행/동작한다.
- 주요 기능 시나리오에서 DB 읽기/쓰기 동작이 정상 확인된다.
- DB 연결/설정 방식이 `../memo`의 마이그레이션된 방식과 일관된다.
- 프로젝트가 Lithent SSR 보일러플레이트 기반으로 구성되고, Tailwind CSS 적용이 완료된다.
- 카드 리스트 + 상세뷰 화면이 정상 동작하고 디자인 품질이 목표 수준으로 개선된다.
- 카드 리스트에서 `title/tag/date` 노출과 카드 전체 클릭 이동이 정상 동작한다.
- 카테고리 필터가 `.env` 값으로 동작하며, 값 변경 후 리스트 결과가 의도대로 바뀌는 것이 확인된다.
- 마크다운 뷰어에서 라이트/다크 모드 전환이 정상 동작하고, 두 모드 모두 GitHub 스타일 기반 가독성이 확보된다.
- 모바일과 PC에서 뷰어 반응형 동작이 확인되며, PC에서는 최대 너비 제한이 적용된다.

## 10. 오픈 이슈
- 상세 디자인 기준(참고 화면/스타일 가이드) 확정 필요.
