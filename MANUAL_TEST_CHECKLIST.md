# MANUAL_TEST_CHECKLIST

## 1. 목적
이 문서는 릴리즈 직전 수동 검증 기준이다.  
`REQUIREMENTS.md`, `DESIGN.md`, `IMPLEMENT.md`의 핵심 수용 조건을 사람 검증 관점에서 체크한다.

## 2. 실행 전 준비
### 2.1 테스트 환경 기록
- 테스트 일시(UTC):
- 테스트 담당자:
- 앱 버전/커밋 SHA:
- 테스트 URL:
- 브라우저/버전:
- OS:

### 2.2 선행 조건
- [ ] `.env`에 `MONGODB_URI`가 올바르게 설정되어 있다.
- [ ] `.env`에 `JMEMO_CATEGORY_FILTER`를 설정할 수 있다.
- [ ] 서버가 기동 가능한 상태다(실행 명령은 `README.md` 기준).
- [ ] 테스트용 데이터(최소 `review`, `share` 카테고리 문서)가 준비되어 있다.

## 3. 최종 판정 규칙
- `P0` 항목은 전부 PASS여야 릴리즈 가능.
- `P1` 항목 FAIL은 릴리즈 차단 사유는 아니지만, 배포 노트에 잔여 이슈로 기록해야 한다.
- FAIL 발생 시 재현 절차와 로그/스크린샷 링크를 반드시 남긴다.

## 4. 테스트 케이스

### A. 기동/헬스체크
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-001 | P0 | 서버를 기동한다. | 프로세스가 정상 시작되고 치명적 에러 없이 대기한다. | [ ] PASS [ ] FAIL |
| MT-002 | P0 | `GET /health/live` 호출 | `200`과 `ok: true` 응답을 반환한다. | [ ] PASS [ ] FAIL |
| MT-003 | P0 | `GET /health/ready` 호출 | DB 연결 가능 시 `200`, 연결 불가 시 `503`을 반환한다. | [ ] PASS [ ] FAIL |

### B. 리스트/상세 기본 동작
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-010 | P0 | 리스트 페이지(`/`) 접속 | 카드 리스트가 렌더링된다(빈 페이지/깨짐 없음). | [ ] PASS [ ] FAIL |
| MT-011 | P0 | favorite/최신 문서가 섞인 데이터 확인 | 리스트 정렬이 `favorite 우선 -> 최신순`으로 보인다. | [ ] PASS [ ] FAIL |
| MT-012 | P0 | 임의 카드 클릭 | 상세 페이지(`/notes/:id`)로 이동하고 본문이 표시된다. | [ ] PASS [ ] FAIL |
| MT-013 | P1 | `/:id` 형태의 레거시 URL 직접 진입 | `/notes/:id`로 정상 리다이렉트된다. | [ ] PASS [ ] FAIL |
| MT-014 | P0 | 잘못된 ID 또는 없는 ID로 상세 진입 | 404 처리(사용자 메시지 포함)가 일관되게 동작한다. | [ ] PASS [ ] FAIL |
| MT-015 | P0 | 카드 정보 표시 확인 | 각 카드에 `title/tag/date`가 표시된다. | [ ] PASS [ ] FAIL |
| MT-016 | P0 | 카드의 모서리/여백 영역 등 임의 위치 클릭 | 카드 내부 어느 위치 클릭해도 상세로 이동한다. | [ ] PASS [ ] FAIL |

### C. 카테고리 필터(.env)
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-020 | P0 | `JMEMO_CATEGORY_FILTER=review`로 실행 | `review` 태그 문서만 리스트에 노출된다. | [ ] PASS [ ] FAIL |
| MT-021 | P0 | `JMEMO_CATEGORY_FILTER=share`로 변경 후 재기동 | 리스트 결과가 `share` 기준으로 변경된다. | [ ] PASS [ ] FAIL |
| MT-022 | P0 | `JMEMO_CATEGORY_FILTER=review,share` 설정 | 두 카테고리 문서가 함께 노출된다. | [ ] PASS [ ] FAIL |
| MT-023 | P0 | `JMEMO_CATEGORY_FILTER`를 빈 문자열로 설정 | 카테고리 필터가 비활성화되어 전체 문서가 노출된다. | [ ] PASS [ ] FAIL |

### D. Markdown Viewer 품질
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-030 | P0 | 제목/리스트/코드블록/표/인용구가 포함된 문서 열기 | 주요 마크다운 요소가 깨짐 없이 렌더링된다. | [ ] PASS [ ] FAIL |
| MT-031 | P0 | 상세 본문 가독성 확인 | GitHub 스타일 계열 타이포그래피로 읽기 좋게 표시된다. | [ ] PASS [ ] FAIL |
| MT-032 | P0 | 본문에 `<script>` 또는 inline handler 포함 데이터 확인 | 위험 스크립트가 실행되지 않고 제거/무력화된다. | [ ] PASS [ ] FAIL |
| MT-033 | P1 | 긴 코드 라인/긴 URL 포함 문서 확인 | 레이아웃 붕괴 없이 스크롤 또는 줄바꿈으로 처리된다. | [ ] PASS [ ] FAIL |

### E. 라이트/다크 모드
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-040 | P0 | 테마 토글을 라이트/다크로 각각 전환 | 두 모드 모두 즉시 반영되고 텍스트 대비가 충분하다. | [ ] PASS [ ] FAIL |
| MT-041 | P0 | 테마 변경 후 새로고침/재접속 | 마지막 선택 모드가 유지된다(`jmemo_theme`). | [ ] PASS [ ] FAIL |
| MT-042 | P1 | 시스템 기본 테마가 다른 환경에서 최초 접속 | 쿠키 미설정 시 `prefers-color-scheme` 기반 초기 모드가 반영된다. | [ ] PASS [ ] FAIL |

### F. 반응형/최대 너비
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-050 | P0 | 모바일 폭(예: 390px)에서 리스트/상세 확인 | 카드/본문 레이아웃 깨짐 없이 1열 중심으로 표시된다. | [ ] PASS [ ] FAIL |
| MT-051 | P0 | 태블릿 폭(예: 768px)에서 리스트/상세 확인 | 중간 폭 레이아웃이 자연스럽게 전환된다. | [ ] PASS [ ] FAIL |
| MT-052 | P0 | 데스크톱 폭(예: 1280px 이상) 상세 확인 | 본문이 과도하게 넓어지지 않고 최대 너비 제한이 적용된다. | [ ] PASS [ ] FAIL |

### G. 운영 관측성/보안
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-060 | P1 | 일반 요청 1회 수행 후 서버 로그 확인 | `time/level/requestId/route/status/latencyMs` 필드가 기록된다. | [ ] PASS [ ] FAIL |
| MT-061 | P1 | DB URI/토큰 관련 로그 확인 | 민감정보가 원문으로 노출되지 않는다(마스킹/비노출). | [ ] PASS [ ] FAIL |
| MT-062 | P1 | DB 불능 상태에서 `ready` 재호출 | `503`과 오류 원인 추적 가능한 로그가 남는다. | [ ] PASS [ ] FAIL |

### H. 종합 스모크
| ID | 우선순위 | 절차 | 기대 결과 | 결과 |
| --- | --- | --- | --- | --- |
| MT-070 | P0 | `리스트 진입 -> 상세 이동 -> 테마 전환 -> 새로고침` | 핵심 사용자 플로우가 중단 없이 동작한다. | [ ] PASS [ ] FAIL |
| MT-071 | P0 | `.env` 필터값 변경 후 재기동 -> 리스트 재확인 | 운영 설정 변경이 코드 수정 없이 서비스 동작에 반영된다. | [ ] PASS [ ] FAIL |

## 5. 결과 요약
- P0 PASS 개수:
- P0 FAIL 개수:
- P1 PASS 개수:
- P1 FAIL 개수:
- 최종 판정: [ ] RELEASE OK [ ] RELEASE BLOCKED

## 6. 결함 기록
| 결함 ID | 연관 테스트 ID | 심각도 | 재현 절차 요약 | 상태 |
| --- | --- | --- | --- | --- |
| BUG-001 | MT-___ | Critical/Major/Minor |  | Open/In Progress/Resolved |

## 7. 증적 링크
- 로그 경로:
- 스크린샷/영상:
- 관련 이슈/PR:

## 8. 서명
- QA/검증자:
- 승인자:
- 완료 시각(UTC):
